# DATA-PIPILINE-SPORTS-CLUB-
A mini data engineering project that models an IPL-style sports system using XML, XSD, XSLT, and Python.  The project stores structured data (teams, players, coaches, sessions, stadiums, bookings) in an XML dataset, validates it using XSD, and transforms it into HTML reports and JSON outputs through XSLT and a Python pipeline.
